{
  "config": {
    "step": {
      "user": {
        "title": "Advanced Downloader",
        "description": "Set the base folder where downloads will be stored.",
        "data": {
          "download_dir": "Base download directory (absolute path, e.g., /media)",
          "overwrite": "Overwrite existing files by default"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Advanced Downloader Options",
        "data": {
          "download_dir": "Base download directory",
          "overwrite": "Overwrite by default",
          "delete_file_path": "Default file path for deletion",
          "delete_directory_path": "Default directory path for deletion"
        }
      }
    }
  }
}
